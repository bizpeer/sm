<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SM Global</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <div class="nav-container">
            <a href="/<%= lang === 'ko' ? '' : lang %>" class="logo">SM Global</a>
            <nav>
                <ul class="nav-links">
                    <li><a href="/<%= lang === 'ko' ? '' : lang %>"><%= t.menu_about %></a></li>
                    <li><a href="/<%= lang === 'ko' ? '' : lang %>#products"><%= t.menu_products %></a></li>
                    <li><a href="/<%= lang === 'ko' ? '' : lang %>/inquiry"><%= t.menu_inquiry %></a></li>
                </ul>
            </nav>
            <div class="lang-switch">
                <a href="/" class="<%= lang === 'ko' ? 'active' : '' %>">KO</a>
                <a href="/en" class="<%= lang === 'en' ? 'active' : '' %>">EN</a>
                <a href="/jp" class="<%= lang === 'jp' ? 'active' : '' %>">JP</a>
                <a href="/cn" class="<%= lang === 'cn' ? 'active' : '' %>">CN</a>
            </div>
        </div>
    </header>
